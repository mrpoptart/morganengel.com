{
  "//": "Local Firebase EMULATOR config only. The live site deploys via Vercel, not `firebase deploy`. The emulators run under a demo-* project id so they can never reach real Firebase.",
  "firestore": {
    "rules": "firestore.rules"
  },
  "storage": {
    "rules": "storage.rules"
  },
  "emulators": {
    "auth": { "host": "127.0.0.1", "port": 9099 },
    "firestore": { "host": "127.0.0.1", "port": 8080 },
    "storage": { "host": "127.0.0.1", "port": 9199 },
    "hub": { "host": "127.0.0.1", "port": 4400 },
    "ui": { "enabled": false },
    "singleProjectMode": true
  }
}
